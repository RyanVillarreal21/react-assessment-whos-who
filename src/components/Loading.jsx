import React from "react";

import LoadingSVG from "../assets/loading.svg";

const Loading = () => <div style={{display: "flex", flexDirection: "column", textAlign: "center"}}><img width={"200px"} src={LoadingSVG} alt="Loading..." />Loading...</div>

export default Loading;
